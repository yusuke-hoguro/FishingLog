# 学習進捗管理テンプレート

このテンプレートは、学習Planの進捗と理解の証拠をテーマごとに記録し、別のセッションでも学習を正確に再開するために使用します。

## 使い方

1. このファイルを `docs/study/progress/<learning-id>-progress.md` にコピーする。
2. 対応する学習Planと同じ学習IDを設定する。
3. 対応するLearning Planと同じ `親Roadmap Step ID` と `親Roadmap Step` を記録する。
4. AIエージェントとの学習開始時に、Planとこの進捗ファイルの両方を読み込ませる。
5. Step完了時とセッション終了時に更新する。
6. 現在Plan、現在Step、確認待ち、次の1アクションが変わるときは `docs/study/learning-state.md` も同期する。

> `<...>` は記入箇所です。不要な例や案内文は、進捗ファイル作成時に削除してください。

---

## AIエージェントへの再開指示

学習を再開するときは、次の順番で確認してください。

1. `docs/study/learning-state.md` を読み、現在の運用範囲、Plan、Step、フェーズを確認する。
2. 現在の運用範囲が `roadmap` の場合だけRoadmapのStatus一覧を読み、親Roadmap StepのStatusを確認し、現在のRoadmap Stepと親Stepが一致して対応するPlanが関連Learning Planに登録されていることを確認する。`independent` の場合はRoadmapへの登録・親Step照合を対象外とする。
3. `対応するPlan` を読み、親Roadmap Step ID、全体のゴール、Stepの依存関係を確認する。
4. 運用範囲によらず、状態ファイルが指定するPlan IDと、このファイルおよび対応するPlanの学習IDを照合し、現在Stepが状態ファイル・Plan・Progressで一致することを確認する。`roadmap` ではPlan・Progressの親Roadmap情報が対象Stepと一致すること、`independent` では状態ファイルの現在Roadmap Step IDとPlan・Progressの親Roadmap Step ID / 親Roadmap Stepがすべて `なし` であることを確認する。
5. このファイルの `現在地`、`未解決事項`、`次回の開始地点` を確認する。
6. 完了済みStepの理解の証拠と、Plan変更履歴を確認する。
7. 学習者に現在地と今回行う1 Stepを短く説明する。
8. `次回最初に行うこと` から再開し、一度に複数Stepへ進まない。

適用対象の照合で不一致がある場合、または状態ファイル・対応するPlan・確定済みPlanに必要なProgressが欠損している場合は作業を進めず、差異を報告して学習ワークフローの復旧手順へ移ってください。独立学習がRoadmapに登録されていないことは不一致ではありません。復帰先のRoadmap情報は現在の独立Planの親ではなく、親Stepの照合に使いません。復帰先自体の整合性は共通ルールに従って別に確認します。

進捗更新時は、次のルールを守ってください。

- 実施した作業と、理解できたことを分けて記録する。
- 成果物が作成されただけではStepを完了にしない。
- 完了条件を満たした証拠と、学習者の完了承認を記録する。
- 理解が曖昧な事項を、完了済みとして扱わない。
- AIの推測で進捗を補完せず、不明な事項は `要確認` と記録する。
- Planを変更した場合は、理由と影響を変更履歴へ残す。
- 親Roadmap Stepとの関係が変わる場合は、PlanとRoadmapも同時に同期する。
- 秘密情報や生の認証情報は記録しない。

---

## 基本情報

| 項目 | 内容 |
| --- | --- |
| 学習ID | `<対応するPlanと同じ、変更しない永続ID>` |
| 学習テーマ | `<学習するテーマ>` |
| 親Roadmap Step ID | `<例: roadmap-step-01>` |
| 親Roadmap Step | `<例: Step 1: PHP基礎>` |
| 対応するPlan | `<例: docs/study/plans/terraform-ec2-instance-plan.md>` |
| 対象成果物 | `<例: docs/architecture/terraform/ec2-instance.md>` |
| 学習開始日 | `YYYY-MM-DD` |
| 最終更新日 | `YYYY-MM-DD` |
| 全体ステータス | `未着手 / 進行中 / 完了 / 保留` |

> Roadmap外の独立学習では、親Roadmap Step ID / 親Roadmap Stepを `なし` とします。

確定済みPlanには必ず対応するProgressを1つだけ作成します。Progress単独で作成せず、学習IDや親Roadmap Stepを後から独立して変更しません。

## 現在地

| 項目 | 内容 |
| --- | --- |
| 現在のStep | `Step <番号>: <タイトル>` |
| Stepの状態 | `未着手 / 進行中 / 完了 / 保留` |
| 現在取り組んでいる内容 | `<内容>` |
| 最後に完了したこと | `<内容>` |
| 次回最初に行うこと | `<1つの具体的なアクション>` |

## Step進捗一覧

| Step | タイトル | 状態 | 完了日 | 完了根拠の要約 |
| --- | --- | --- | --- | --- |
| 1 | `<タイトル>` | `未着手` | `-` | `-` |
| 2 | `<タイトル>` | `未着手` | `-` | `-` |
| 最終 | 復習・応用 | `未着手` | `-` | `-` |

状態は `未着手 / 進行中 / 完了 / 保留` のいずれかを使用します。

同時に `進行中` にできるStepは1つだけです。Step完了をユーザーが確認するまでは、次のStepを `進行中` にしません。

## 現在のStepの詳細

### Step `<番号>`: `<タイトル>`

#### 今回の目的

`<Planに記載された目的>`

#### 実施した作業

- `<調査、設計、実装、回答など>`

#### 作成・変更した成果物

- `<ファイル、見出し、変更内容。未反映の場合はその旨>`

#### 理解できたこと

- `<理解した概念や仕組み>`

#### 自分の言葉で説明できたこと

- `<説明した内容の要約>`

#### AIからの指摘と修正内容

- 指摘: `<誤解、不足、改善点>`
- 修正: `<回答または成果物をどのように修正したか>`

#### まだ曖昧なこと

- `<追加説明や再演習が必要な内容。なければ「なし」>`

#### 完了条件の確認

- [ ] `<Planに記載された作業・成果物の条件>`
- [ ] `<Planに記載された理解の条件>`
- 確認方法: `<説明、回答、差分、検証結果など>`
- 完了根拠: `<条件を満たしたと判断できる具体的な内容>`
- 学習者の確認: `未確認 / 確認済み`

#### 次のアクション

`<次に行う1つの具体的な作業>`

## 完了Stepの記録

<!-- Stepを完了するたびに、以下のブロックを複製する。 -->

### Step `<番号>`: `<タイトル>`

- 完了日: `YYYY-MM-DD`
- 実施した作業: `<要約>`
- 成果物への反映: `<ファイルと見出し>`
- 理解できたこと: `<要約>`
- 自分の言葉で説明したこと: `<要約>`
- 完了を確認した方法: `<質問、演習、検証など>`
- 完了根拠: `<具体的な回答・結果の要約>`
- 残っている注意点: `<なければ「なし」>`

## 理解の記録

### 説明できるようになったこと

- `<項目と簡潔な説明>`

### 重要な設計判断

| 判断 | 採用理由 | 前提・制約 | 参照先 |
| --- | --- | --- | --- |
| `<判断>` | `<理由>` | `<前提>` | `<成果物の見出しなど>` |

### 検討した代替案

| 代替案 | メリット | デメリット | 不採用理由 |
| --- | --- | --- | --- |
| `<案>` | `<内容>` | `<内容>` | `<理由>` |

### 条件が変わった場合の影響

| 条件変更 | 影響する設計 | 対応方針 |
| --- | --- | --- |
| `<変更内容>` | `<影響>` | `<対応>` |

## 未解決事項・ブロッカー

| 種別 | 内容 | 解決に必要なこと | 状態 |
| --- | --- | --- | --- |
| `疑問 / 理解不足 / 環境 / その他` | `<内容>` | `<調査、説明、確認など>` | `未解決 / 確認中 / 解決` |

## Plan変更履歴

| 日付 | 変更内容 | 変更理由 | 後続Stepへの影響 | 親Roadmap Stepへの影響 | 学習者確認 |
| --- | --- | --- | --- | --- | --- |
| `YYYY-MM-DD` | `<追加・削除・順序変更など>` | `<理由>` | `<影響>` | `<なし / あり: 内容>` | `確認済み` |

## 復習・応用の記録

### 資料を見ない説明

- 実施日: `YYYY-MM-DD`
- 説明できたこと: `<内容>`
- 説明できなかったこと: `<内容>`

### 代替案との比較

- 問い: `<問い>`
- 回答の要約: `<回答>`
- 再確認事項: `<なければ「なし」>`

### 条件変更問題

- シナリオ: `<変更された条件>`
- 回答の要約: `<影響と対応方針>`
- 再確認事項: `<なければ「なし」>`

### 障害対応問題

- 想定障害: `<障害>`
- 切り分け方: `<確認順序と判断理由>`
- 再確認事項: `<なければ「なし」>`

### AIによる確認問題

| # | 問題 | 回答の要約 | 判定 | 補足 |
| --- | --- | --- | --- | --- |
| 1 | `<問題>` | `<回答>` | `理解済み / 要復習` | `<補足>` |

## 今後の学習候補

- `<今回の対象外だった内容、発展テーマ、理解不足だった内容>`

## 次回の開始地点

### 次回最初に行うこと

`<ファイルや問いまで特定した、1つの具体的なアクション>`

### 再開前に確認するもの

- `<PlanのStepまたは参照資料>`

### AIに引き継ぐ注意事項

- `<説明方法、未解決事項、Planとのずれなど>`
